spring:
  application:
    name: myFirstProject

  data:
    mongodb:
      uri: mongodb+srv://myname:myPassword@cluster0.hz9ezxb.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      database: journaldb
      auto-index-creation: true

  main:
    allow-circular-references: true

server:
  port: 8090
  servlet:
    context-path: /journal
